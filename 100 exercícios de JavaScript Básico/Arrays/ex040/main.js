// Crie um array de frutas e imprima uma lista numerada.

const frutas = ["banana", "maçã", "uva", "melancia", "abacaxi"]

for (i = 0; i < frutas.length; i++) {
    console.log(`${i + 1}. ${frutas[i]}`)
}